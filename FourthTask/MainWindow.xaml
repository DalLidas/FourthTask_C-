<Window x:Class="FourthTask.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FourthTask.ViewModels"
        xmlns:local="clr-namespace:FourthTask"
        mc:Ignorable="d"
        Title="{Binding Title}" 
        Height="600" Width="1200" MinHeight="550" MinWidth="550"
        FontFamily="Bahnschrift" FontSize="18" 
        Icon="/Static/Images/train-menu-icon.png">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Grid Height="auto" Width="auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">       
        <Frame
            Grid.RowSpan="2"
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" 
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"  
            Name="_mainFrame" 
            NavigationUIVisibility="Hidden"/>

    </Grid>

</Window>
